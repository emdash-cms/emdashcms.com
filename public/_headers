# Fingerprinted assets emitted by Astro (JS, CSS, fonts, images, etc.)
# Filenames include a content hash, so they're safe to cache forever.
/_astro/*
  Cache-Control: public, max-age=31536000, immutable
