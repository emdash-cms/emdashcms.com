<figure class="agent-journey">
	<div class="journey-label"><span class="live-dot"></span> A shared content workflow <span class="journey-path">agent / content / editor</span></div>
	<div class="journey-canvas" aria-hidden="true">
		<svg class="journey-wires" viewBox="0 0 670 430" preserveAspectRatio="none" fill="none">
			<path d="M252 137H390Q422 137 422 169V194" />
			<path d="M422 262V300Q422 328 394 328H302" />
			<path class="travel" d="M252 137H390Q422 137 422 169V194" />
			<path class="travel travel-late" d="M422 262V300Q422 328 394 328H302" />
		</svg>
		<div class="agent-note">
			<div class="card-kicker"><span class="agent-glyph">✳</span> Agent via MCP <span class="note-time">now</span></div>
			<p>“Draft a launch update in Posts. Keep it unpublished.”</p>
			<div class="note-operation"><span>✓</span> content_create <span>→ draft</span></div>
		</div>
		<div class="content-card">
			<div class="card-kicker"><span class="content-glyph">▤</span> EmDash / Posts <span class="draft-pill">Draft</span></div>
			<strong>A new chapter for our team</strong>
			<div class="content-line"></div>
			<div class="content-line short"></div>
			<div class="content-fields"><span>Title</span><span>Excerpt</span><span>Content</span></div>
		</div>
		<div class="review-card">
			<div class="card-kicker"><span class="review-glyph">◉</span> Editor review <span class="review-action">Compare changes ↗</span></div>
			<div class="review-columns"><div><span>LIVE</span><b class="old-line"></b><b class="old-line short"></b></div><div><span>DRAFT</span><b class="new-line"></b><b class="new-line short"></b></div></div>
		</div>
	</div>
	<figcaption>Illustrative workflow: an agent creates a draft; a person can compare it with the published version before choosing what to do next.</figcaption>
</figure>

<style>
	.agent-journey {
		--line: color-mix(in srgb, var(--color-text) 13%, transparent);
		--muted: color-mix(in srgb, var(--color-text) 53%, transparent);
		position: relative;
		min-width: 0;
		overflow: hidden;
		margin: 0;
		background: radial-gradient(ellipse at 62% 48%, var(--heat-12), transparent 50%), var(--color-surface);
		border: 1px solid var(--color-border);
		border-radius: var(--radius-lg);
	}
	.agent-journey::before {
		position: absolute;
		inset: 0;
		background-image: radial-gradient(var(--line) 0.7px, transparent 0.7px);
		background-size: 19px 19px;
		content: "";
		mask-image: radial-gradient(ellipse at center, black, transparent 73%);
	}
	.journey-label {
		position: relative;
		z-index: 1;
		display: flex;
		align-items: center;
		gap: 0.6rem;
		padding: 1.3rem 1.6rem;
		color: var(--muted);
		font-size: 0.72rem;
		font-weight: 550;
	}
	.live-dot { width: 0.42rem; height: 0.42rem; flex: none; background: var(--color-primary); border-radius: 50%; box-shadow: 0 0 0 4px var(--heat-12); }
	.journey-path { margin-inline-start: auto; font-family: ui-monospace, SFMono-Regular, monospace; font-size: 0.62rem; white-space: nowrap; }
	.journey-canvas { position: relative; min-height: 28rem; }
	.journey-wires { position: absolute; inset: 0; width: 100%; height: 100%; pointer-events: none; }
	.journey-wires path { stroke: var(--line); stroke-width: 2; }
	.journey-wires .travel { stroke: var(--color-primary); stroke-dasharray: 5 9; }
	.agent-note, .content-card, .review-card {
		position: absolute;
		background: var(--color-surface);
		border: 1px solid var(--color-border);
		border-radius: 0.75rem;
		box-shadow: 0 16px 45px color-mix(in srgb, var(--color-text) 9%, transparent);
	}
	.agent-note { top: 4%; left: 5%; width: 47%; padding: 1.25rem; transform: rotate(-2deg); }
	.content-card { top: 29%; right: 5%; width: 48%; padding: 1.25rem; transform: rotate(2deg); }
	.review-card { bottom: 5%; left: 11%; width: 52%; padding: 1.1rem; transform: rotate(-1deg); }
	.card-kicker { display: flex; align-items: center; gap: 0.45rem; color: var(--muted); font-size: 0.66rem; font-weight: 600; }
	.agent-glyph, .content-glyph, .review-glyph { color: var(--color-primary); font-size: 1rem; line-height: 1; }
	.note-time, .draft-pill, .review-action { margin-inline-start: auto; font-size: 0.57rem; font-weight: 500; white-space: nowrap; }
	.note-time { color: var(--muted); }
	.agent-note p { max-width: 14rem; margin-top: 1rem; font-size: 0.8rem; font-weight: 550; line-height: 1.55; }
	.note-operation { display: flex; align-items: center; gap: 0.4rem; margin-top: 1rem; padding: 0.55rem 0.6rem; color: var(--color-primary); background: var(--heat-8); border-radius: 0.35rem; font-family: ui-monospace, SFMono-Regular, monospace; font-size: 0.63rem; }
	.note-operation span:last-child { margin-inline-start: auto; color: var(--muted); }
	.draft-pill { padding: 0.22rem 0.5rem; color: var(--color-primary); background: var(--heat-8); border-radius: 1rem; }
	.content-card strong { display: block; margin: 1.25rem 0 0.95rem; font-size: 0.95rem; letter-spacing: -0.02em; }
	.content-line { height: 0.28rem; margin: 0.5rem 0; background: var(--line); border-radius: 1rem; }
	.content-line.short { width: 64%; }
	.content-fields { display: flex; gap: 0.4rem; margin-top: 1.1rem; }
	.content-fields span { padding: 0.25rem 0.42rem; color: var(--muted); background: var(--color-bg); border: 1px solid var(--line); border-radius: 0.25rem; font-size: 0.57rem; }
	.review-action { color: var(--color-primary); }
	.review-columns { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 1rem; margin-top: 1.15rem; }
	.review-columns > div { display: flex; flex-direction: column; gap: 0.45rem; padding: 0.7rem; background: var(--color-bg); border: 1px solid var(--line); border-radius: 0.45rem; }
	.review-columns span { margin-bottom: 0.15rem; color: var(--muted); font-family: ui-monospace, SFMono-Regular, monospace; font-size: 0.54rem; letter-spacing: 0.06em; }
	.review-columns b { display: block; height: 0.3rem; background: var(--line); border-radius: 1rem; }
	.review-columns .short { width: 64%; }
	.review-columns .new-line { background: var(--heat-40); }
	.agent-journey figcaption { position: relative; padding: 0.5rem 1.6rem 1.35rem; color: var(--muted); font-size: 0.64rem; line-height: 1.5; }
	@media (max-width: 600px) {
		.journey-label { padding-inline: 1rem; }
		.journey-path { display: none; }
		.journey-canvas { min-height: 30rem; }
		.journey-wires { display: none; }
		.agent-note { top: 1%; left: 5%; width: 72%; }
		.content-card { top: 32%; right: 4%; width: 73%; }
		.review-card { bottom: 1%; left: 4%; width: 76%; }
		.agent-journey figcaption { padding-inline: 1rem; }
	}
	@media (prefers-reduced-motion: no-preference) {
		.journey-wires .travel { animation: flow 6s linear infinite; }
		.journey-wires .travel-late { animation-delay: -3s; }
	}
	@keyframes flow { to { stroke-dashoffset: -28; } }
</style>
